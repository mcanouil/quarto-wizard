# The API reference is generated from TypeDoc output by
# scripts/process-api-docs.mjs, so these pages have no editable source; drop the
# "Edit this page" and "Report an issue" links the site adds elsewhere.
repo-actions: false
