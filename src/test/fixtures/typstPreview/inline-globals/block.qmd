---
engine: markdown
format: html
filters:
  - typst-render
extensions:
  typst-render:
    output-directory: ./recorded
    output-source: true
    width: 6cm
    margin: 1cm
    background: "#faf6ee"
    preamble: "#let accent = red"
---

Value: `{typst} #text(fill: accent)[Hi]`.
